rootProject.name = "qrx" //change this to break jitpack.io build
include ':qrx'
project(':qrx').name = 'qrx'
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
}